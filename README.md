# meghbrishti

Birthday website scaffold built with Vite.

## Run locally

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

## Deploy

A GitHub Actions workflow is included at `.github/workflows/deploy.yml` to deploy the `dist` output to GitHub Pages on pushes to `main`.
